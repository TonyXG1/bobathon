[project]
name = "contracts"
version = "0.1.0"
description = "Shared Pydantic models and JSON schemas for Regulatory Radar"
requires-python = ">=3.12"

dependencies = [
    "pydantic>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
